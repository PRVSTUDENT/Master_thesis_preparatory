Name: VS Code Copilot Agent/ Chatgpt Codex Agent

Summary:
- Purpose: A workspace-scoped agent that follows chat instructions, may run terminal commands and edit files, and enforces a strict "mirror" of any created/edited files into a single flat tracking directory for easy upload/share with ChatGPT.

Role & Privileges:
- Runs in the user's VS Code environment with permission to edit files in the workspace and to run terminal/OS commands when explicitly requested by the chat.
- Prefer non-destructive operations (create commits, new branches) for large changes; overwrites allowed only when explicitly requested.

When to pick this agent:
- Use when the task requires automated file edits plus an immediate, reproducible snapshot of files touched by the agent (for quick sharing or review).

General behavior:
- Follow high-level instructions given in the chat, including running necessary commands in the integrated terminal.
- Keep actions transparent by describing major steps (for example: "running command X", "editing files Y, Z").
- Prefer non-destructive operations where reasonable; do safe commits or create a branch for larger refactors unless user requests direct overwrite.

Stage 13 HPC handoff:
- Before doing any new work on the Stage 13 5000-cycle Abaqus/HPC run, read `docs/stage13_hpc_handoff_2026-05-16.md`.
- Treat that handoff as the current source of truth for SSH config, remote HPC path, PBS queue constraints, job ID, monitoring commands, and next steps.
- Do not delete or clean generated Abaqus files unless the user explicitly asks; the workspace contains many generated and untracked run outputs.

HPC job timing and resource reporting:
- Whenever an HPC/PBS job is submitted, monitored while running, or checked after completion, report the exact timing and resource information needed for planning future long runs.
- For running jobs, use `qstat -f <job_id>` and include at minimum: `job_state`, queue, requested resources (`Resource_List.select`, `Resource_List.ncpus`, `Resource_List.mpiprocs`, `Resource_List.mem`, `Resource_List.walltime`), `resources_used.walltime`, `resources_used.cput`, `resources_used.cpupercent`, `resources_used.mem`, `resources_used.vmem`, `resources_used.ncpus`, `ctime`, `stime`, and current host/vnode if available.
- For finished jobs, use `qstat -x -f <job_id>` or the best available PBS history command and include: `Exit_status`, final `resources_used.walltime`, `resources_used.cput`, `resources_used.cpupercent`, `resources_used.mem`, `resources_used.vmem`, `resources_used.ncpus`, requested resources, `ctime`, `stime`, `mtime`/finish time if available, and whether the job completed successfully.
- When useful, also report the controller/status-file progress fields from the run directory, for example `progress_done`, `progress_total`, `current_task`, and the tail of the main controller log.
- Always present these values with concrete timestamps and job IDs, not only relative descriptions such as "finished quickly" or "used little memory".

Stage 16N two-job / 32-core scheduling policy:
- From 2026-06-06 onward, plan Stage 16N solver work assuming at most 2 simultaneous Abaqus jobs.
- Each production solver job should request 16 CPU cores with 1 MPI rank x 16 OpenMP threads and a maximum walltime of 24 hours.
- This means the maximum intended active Stage 16N usage is 2 jobs x 16 CPUs = 32 CPU cores.
- Use the two slots intelligently: first-time gate cases run alone until compared; once a method class passes, submit two independent cases together.
- Current fixed-jump gate rule: B1 must finish and be compared before submitting B2/B3. If B1 passes, submit B2 and B3 simultaneously. If B1 fails, hold B2/B3 and use the two slots for smaller/debug variants instead.

Local performance / Git hygiene for Abaqus workspace:
- Problem observed on 2026-06-13: after restart the WAVELINK/SMI USB second display was smooth until Codex started working in `D:\TUBAF\Master_Thesis\Abaqus_trial`. The lag was caused by Codex/Git scanning and hashing large Abaqus generated outputs and many untracked run folders, not by normal RAM exhaustion.
- Diagnostic evidence: live process checks showed Codex spawning multiple `git hash-object` processes under `runs/chaboche_umat/stage12_percentage_jump_1000cycles`; the repo also had about 1.47 GiB of loose Git objects and many generated `.odb`/`.sim`/`.dat`/`.sta`/`.023`/`.cax` artifacts. A later `git gc` attempt made the problem worse by running `git pack-objects` at high CPU with about 7.5 GB RAM, so it was stopped.
- Immediate fix applied: expanded `.gitignore` for Abaqus generated files and local/HPC bundles, enabled local Git untracked cache with `git config --local core.untrackedCache true`, and set `git config --local status.showUntrackedFiles no`.
- Follow-up fix applied on 2026-06-14: the agent handoff mirror `runs/chaboche_umat/new files created or edited/` was also causing Codex/Git lag because copied handoff artifacts were tracked or diffed. The folder is now ignored in `.gitignore`; existing tracked mirror copies were locally marked `skip-worktree` so Git status stays focused on real source files.
- Future agents must not run broad Git operations that enumerate all untracked files by default in this repo. Prefer `git status --short --untracked-files=no` and only use `git status --untracked-files=all` when the user explicitly asks to inspect untracked outputs.
- Future agents must keep `runs/chaboche_umat/new files created or edited/` as a local handoff-only folder. Do not stage, commit, diff, or hash files from that mirror folder unless the user explicitly asks for the mirror contents to be versioned.
- Avoid `git gc`, `git repack`, broad recursive file-size scans, or full-tree `rg`/hashing across generated Abaqus run folders during interactive work unless the user explicitly approves a long/heavy maintenance step.
- Keep solver outputs, scratch directories, and upload mirrors out of Git. Treat generated Abaqus artifacts as reproducible run data, not source, unless the user explicitly says a specific file should be tracked.

Remote Git offload to HPC:
- Prefer running heavy Git operations on the HPC clean clone when possible, especially commands that enumerate, hash, repack, garbage-collect, or inspect large portions of the repository.
- The HPC cannot directly operate on the Windows path `D:\TUBAF\Master_Thesis\Abaqus_trial`; use GitHub as the synchronization bridge between the local Windows editing workspace and the HPC clone.
- Recommended HPC clone location: `/scratch/$USER/git_work/Master_thesis_preparatory`.
- Standard HPC setup:
  ```bash
  mkdir -p /scratch/$USER/git_work
  cd /scratch/$USER/git_work
  git clone https://github.com/PRVSTUDENT/Master_thesis_preparatory.git
  cd Master_thesis_preparatory
  git checkout copilot/curved-cephalopod
  git pull --ff-only
  ```
- From Windows, run targeted remote Git checks through SSH, for example:
  ```powershell
  ssh -F $env:USERPROFILE\.ssh\codex_config tu_freiberg "cd /scratch/\$USER/git_work/Master_thesis_preparatory && git status --short --untracked-files=no"
  ```
- Use the HPC clone for heavy maintenance such as `git count-objects -vH`, `git fsck`, and `git gc`, but only after local commits have been pushed to GitHub and pulled into the HPC clone.
- If commits are created on the HPC clone, push them to GitHub and then pull them back locally with `git pull --ff-only`.
- Do not run Git inside scratch Abaqus run directories containing heavy `.odb`, `.stt`, `.sim`, `.res`, `.mdl`, `.prt`, `state.bin`, or large `state.csv` files.
- Local Windows Git usage should remain targeted: prefer `git status --short --untracked-files=no`, `git diff --name-only`, and explicit `git add <file1> <file2>`.

File tracking and mirroring (required behavior):
1. For every file created or edited by the agent during a single operation, identify the file's workspace-relative path.
2. Prefer automation: run `D:\TUBAF\Master_Thesis\Abaqus_trial\runs\chaboche_umat\sync_new_or_edited_files.py` with the list of created/edited files whenever viable.
   - Standard invocation (from workspace root):
     `python runs\chaboche_umat\sync_new_or_edited_files.py <file1> <file2> ...`
   - Path rule for this script: paths under `runs\chaboche_umat` may be passed as relative paths; files outside that tree should be passed as absolute paths.
   - This script is the default mirror mechanism when it can be executed successfully.
   - Viable means: Python is available, the script exists, and the agent has a concrete file list for the current operation.
   - If not viable, perform the manual mirror procedure in steps 3-5.
3. Ensure the tracking directory exists: `D:\TUBAF\Master_Thesis\Abaqus_trial\runs\chaboche_umat\new files created or edited`.
4. Delete all existing files (but not the directory itself) inside that tracking directory before copying the just-created/edited files.
   - Do not create subfolders inside this directory. The directory must be flat.
5. Copy each newly created or edited file into that tracking directory, preserving only the file name (no subfolder structure). Overwrite if the name already exists.
6. If multiple files were edited in the same operation, repeat step 5 for each file so the directory contains only the most-recently-edited files for that operation.

Constraints & safety:
- Never create subfolders inside the mirror directory.
- Do not copy or delete files outside the workspace except when explicitly instructed by the user.
- Refuse commands that would have destructive side effects beyond the workspace (e.g., wiping a drive); ask for clarification.

Edge cases & assumptions (confirmed):
- Binary / large files: by default the agent will NOT mirror binary or large generated outputs (e.g., `.odb`, `.prt`, `.dat`). The mirror will contain only source/text files (code, scripts, configs, text docs) unless the user explicitly requests otherwise.
- Large source files: if a newly created or edited text file is unusually large, the agent will warn and ask whether to copy it into the mirror directory.
- External outputs (e.g., run-generated `.odb` or `.dat`): these will not be copied into the mirror directory unless the user explicitly requests them.

Example workflow (agent-run):
- User: "Edit `umat/chaboche_vp_v1_working.f` to fix X."
- Agent: applies edits, commits (or not, per user), then empties `runs/chaboche_umat/new files created or edited/` and copies `chaboche_vp_v1_working.f` into it as `chaboche_vp_v1_working.f`.

Ambiguities to confirm (questions for the user):
1. Should the agent mirror binary or large generated files (e.g., `.odb`, `.prt`, `.dat`) by default, or only source/text files?

Mirror policy (confirmed): The agent will always keep only files from the immediately previous operation in the tracking directory; it will not preserve a history or create timestamped copies.

Example prompts to try this agent:
- "Use this agent to apply the patch in `umat/` and mirror the changed files."
- "Create a new analysis script `runs/scripts/run_sensitivity.bat` and mirror it."

Suggested follow-ups:
- Add a post-edit wrapper that always calls `sync_new_or_edited_files.py` with the operation file list.
- Optionally add a small manifest file in the mirror directory listing the original paths of mirrored files and the operation timestamp.

Version: 1.0
Author: Generated per user instruction

Automated Stage LaTeX Report (new rules):
- Purpose: For each logical "stage" of work the agent or user performs, create and maintain a single LaTeX document that collects all artifacts (results, images, tables, failures, logs, discussion, conclusions) so it can be converted to PDF later.
- File naming & location: create `reports/stage<NAME>_report.tex` (workspace-relative `reports/` folder). Use a dash/underscore-free `<NAME>` (for example: `stage15k_report.tex`).
- Document structure: the agent should ensure the LaTeX file contains a minimal preamble and the following top-level sections (create if missing):
   - `\\section{Overview}` — short description of the stage and commands used to run it.
   - `\\section{Results}` — include numeric summaries and key metrics.
   - `\\section{Images}` — use `\\includegraphics` from a `reports/images/` subfolder (agent should copy images used into this folder).
   - `\\section{Tables}` — include tabular summaries produced by scripts.
   - `\\section{Failures and Debugging}` — paste relevant log excerpts and brief notes.
   - `\\section{Conclusion and Next Steps}` — short conclusions and recommended next actions.
- Update policy: the agent should append new content to the existing stage LaTeX file or update named subsections (for example, update the `Results` subsection when new numbers are available). Do NOT create a new file per run — keep one file per stage and keep it live-editable.
- Image handling: copy image files referenced into `reports/images/` and reference them with relative paths from the LaTeX file.
- Conversion to PDF: user will run `pdflatex`/`latexmk` manually; the LaTeX files must compile with standard TeX distributions (include `\\usepackage{graphicx,booktabs,hyperref}` in preamble).

HPC login profile (quick reference):
- Use the workspace SSH/login profile when interacting with the remote HPC. Example invocation (PowerShell):

   $env:USERPROFILE\\.ssh\\codex_config tu_freiberg

- The agent should use that profile string as the canonical example to document and, when asked explicitly, run remote commands via the user's preferred SSH configuration.

Agent-run automatic update block (must be replaced on each run):
- Purpose: keep a concise, single updated summary in this `.agent.md` so the agent can quickly read and update current/future tasks and job numbers without growing the file unbounded.
- Implementation: the agent MUST find and replace the entire content between the following markers on each run. The agent should only replace the inner block (not the markers themselves).

<!-- AGENT_RUN_SUMMARY_START -->
Current tasks:
- Keep the Stage 16N report live and only add lightweight evidence files, summaries, and conclusions.
- Keep HPC-first Git usage for merge-sensitive or heavy Git work; the Windows checkout should stay targeted and quiet.
- Do not submit R4J9/R4J10 yet. The R4E exact-target controls failed even after the CSV handoff was verified.
- Prepare/submit/classify R4F full-target native restart controls before any additional extrapolated refinement.

Current jobs:
- Running: none known.
- Completed:
   - `1345655.mmaster02`: R4E1 exact-target control `250 -> exact 280 -> 500`, source solve 251--280, true-skip solve 281--500, `Exit_status=0`, `mnode100`, `resources_used.walltime=05:08:21`, `resources_used.cput=22:15:38`, `resources_used.cpupercent=593`, `resources_used.mem=94375852kb`, comparison `500,fail,1.1886389,11.829104,0.92936729`.
   - `1345656.mmaster02`: R4E2 exact-target control `500 -> exact 505 -> 750`, source solve 501--505, true-skip solve 506--750, `Exit_status=0`, `mnode101`, `resources_used.walltime=05:17:40`, `resources_used.cput=22:34:55`, `resources_used.cpupercent=598`, `resources_used.mem=94371840kb`, comparison `750,fail,1.4217578,13.598377,13.011616`.
   - `1345562.mmaster02`: R4J7 `250 -> 280 -> 500`, true skip `+30`, `Exit_status=0`, `mnode100`, `resources_used.walltime=04:41:23`, `resources_used.cput=19:50:24`, `resources_used.cpupercent=557`, `resources_used.mem=94375932kb`, comparison `500,fail,1.1886389,11.829104,0.92936729`.
   - `1345563.mmaster02`: R4J8 `500 -> 505 -> 750`, true skip `+5`, `Exit_status=0`, `mnode101`, `resources_used.walltime=05:11:31`, `resources_used.cput=21:49:48`, `resources_used.cpupercent=592`, `resources_used.mem=94371840kb`, comparison `750,fail,1.4217578,13.598377,13.011616`.
   - `1345651.mmaster02` and `1345652.mmaster02`: accidental R4J3/R4J4 submissions from the hardcoded plus20 watchdog; immediately stopped with `qdel`, `Exit_status=271`, walltime under one minute.
   - `1345653.mmaster02` and `1345654.mmaster02`: first R4E setup attempt, failed before Abaqus solve because exact target states were incorrectly extracted from the restart-source ODB (`cycle 280` had no field frame; `cycle 505` absent).
- Queued: none.
- Scratch root: `/scratch/pr21vyci/stage16n_scratch_runs_r4e_exact_controls/Abaqus_trial`.

Current scientific status:
- R4E1/R4E2 both completed at the solver level but failed the exact-target control classification.
- R4E1 is controlled by `HOLE_RING_SDV8_MAX=11.829104%`; R4E2 is controlled by `HOLE_RING_MISES_MAX=13.598377%`, with diagnostic `HOLE_RING_S11_MAX_ABS=13.011616%`.
- The R4E comparison summaries are identical to R4J7/R4J8, but the retained exact-target CSVs are byte-identical to the `state.csv` files consumed by the R4E decks.
- R4E exact state hashes: cycle 280 `4facbc0e87caf9c63febb31e51db819308cd496244ec33dabe79ff9a6ed2d3ff`; cycle 505 `b9254d54f41612772a55ee103b42db30734dcc8115d35800b61858d6b50a8948`.
- R4E exact states are not the same as R4J extrapolated states: R4E1/R4J7 max absolute difference `70.03573608398443` in `S1`; R4E2/R4J8 max absolute difference `30.373046875` in `S1`.
- R4E overwrite traces are phase-consistent at diagnostic level: R4E1 `KSTEP=251`, R4E2 `KSTEP=501`; trace lines are sparse diagnostics, not a full point count.
- Current interpretation: material-only overwrite may be insufficient because Abaqus restart still carries the checkpoint-cycle mechanical/internal restart state.
- R4J7: solver success but `fail` at cycle 500 with max primary local error `11.829104%`; the 250-branch jump is now bracketed between `+20` and `+30`.
- R4J8: solver success but `fail` at cycle 750 with max primary local error `13.598377%`; the 500-branch jump is now below `+5`.
- Audit warning: the result is non-monotonic. R4J7 `+30` is worse than R4J5 `+35`, and R4J8 `+5` is worse than R4J6 `+10`.
- Clean conclusion for now: run R4F full-target native restart controls before continuing extrapolated refinement.

Prepared R4F controls:
- Generator: `runs/chaboche_umat/stage16_abaqus_inhomogeneous_cycle_jump_benchmark/prepare_stage16n_r4f_full_target_restart_controls.py`.
- Dispatcher: `runs/chaboche_umat/stage16_abaqus_inhomogeneous_cycle_jump_benchmark/submit_stage16n_r4f_full_target_restart_case.sh`.
- Manifest: `runs/chaboche_umat/stage16_abaqus_inhomogeneous_cycle_jump_benchmark/stage16n_restart_control/full_target_restart_cases/stage16n_r4f_full_target_restart_cases.csv`.
- `R4F1_250_to_280_fullrestart_solve_281_to_500`: source solve 250 -> 280, then full native restart solve 281--500, no overwrite.
- `R4F2_500_to_505_fullrestart_solve_506_to_750`: source solve 500 -> 505, then full native restart solve 506--750, no overwrite.

Future tasks:
- Submit R4F1/R4F2 on the HPC clean clone and classify them with the same `pass/review/fail` primary-local rule.
- If R4F passes, report material-only overwrite as the likely R4E/R4J limitation and only then consider R4J9/R4J10.
- If R4F fails, audit native-restart continuation and comparison mapping before more jump refinement.
- Keep copying only lightweight evidence into GitHub: status markdown, comparison CSVs, selected cycle CSVs, qstat history, PBS stdout, and small logs.
- After the R4J bracket settles, run the material-model verification task: single-element Abaqus UMAT cyclic test versus NEML or an independent Chaboche material-point reference.
- Keep heavy Abaqus files (`.odb`, `.stt`, `.sim`, `.res`, `.mdl`, `.prt`, `state.bin`, large `state.csv`) in `/scratch`, not GitHub or `/home`.

Recent completed job numbers:
- 1342923: R3J1 +5, passed exactly at cycle 500 after follow-up comparison.
- 1342924: R3J2 +5, passed exactly at cycle 750 after follow-up comparison.
- 1344231: R3J3 +10, passed exactly at cycle 500.
- 1344232: R3J4 +10, passed exactly at cycle 750.
- 1344614: R3J5 +20, passed exactly at cycle 500.
- 1344615: R3J6 +20, passed exactly at cycle 750.
- 1344946: R4J1 true +50 cycle-skip scratch job, solver completed, comparison failed at cycle 500 with 14.384123% max primary local error.
- 1344947: R4J2 true +50 cycle-skip scratch job, solver completed, comparison failed at cycle 750 with 14.426805% max primary local error.
- 1344960: R4J3 true +20 cycle-skip scratch job, solver completed, comparison passed at cycle 500 with 3.9830029% max primary local error.
- 1344961: R4J4 true +20 cycle-skip scratch job, solver completed, comparison failed at cycle 750 with 14.147994% max primary local error.
- 1345009: first R4J5 scratch wrapper attempt, exited immediately because native restart source was missing in first scratch staging.
- 1345010: first R4J6 scratch wrapper attempt, exited immediately because native restart source was missing in first scratch staging.
- 1345011: R4J5 true +35 cycle-skip production job, solver completed, comparison review/fail at cycle 500 with 6.9643175% max primary local error.
- 1345012: R4J6 true +10 cycle-skip production job, solver completed, comparison review/fail at cycle 750 with 7.2880782% max primary local error.
- 1345560: first R4J7 +30 submission, setup-only failure with `Exit_status=2` because local generator temporarily lost restart-source fallback.
- 1345561: first R4J8 +5 submission, setup-only failure with `Exit_status=2` because local generator temporarily lost restart-source fallback.
- 1345562: R4J7 true +30 cycle-skip production job, solver completed, comparison failed at cycle 500 with 11.829104% max primary local error.
- 1345563: R4J8 true +5 cycle-skip production job, solver completed, comparison failed at cycle 750 with 13.598377% max primary local error.
- 1345651: accidental R4J3 resubmission from hardcoded plus20 watchdog, stopped with `qdel`, `Exit_status=271`.
- 1345652: accidental R4J4 resubmission from hardcoded plus20 watchdog, stopped with `qdel`, `Exit_status=271`.
- 1345653: first R4E1 exact-target setup attempt, failed before solver because exact cycle-280 field output was not available in the restart-source ODB.
- 1345654: first R4E2 exact-target setup attempt, failed before solver because cycle 505 was not present in the restart-source ODB.
- 1345655: corrected R4E1 exact-target control, solver completed, comparison failed at cycle 500 with 11.829104% max primary local error.
- 1345656: corrected R4E2 exact-target control, solver completed, comparison failed at cycle 750 with 13.598377% max primary local error.

Last updated: 2026-06-16 17:30 Europe/Berlin
<!-- AGENT_RUN_SUMMARY_END -->

Agent update rules for the run-summary block:
- On every agent run that takes action or inspects HPC jobs, the agent will:
   1. Collect the current tasks, planned future tasks, and any job IDs it submitted or monitored.
   2. Construct a concise replacement text for the block between `<!-- AGENT_RUN_SUMMARY_START -->` and `<!-- AGENT_RUN_SUMMARY_END -->`.
   3. Overwrite only the inner content between the markers (delete old inner content and insert the new text) so the file length and structure remain stable.
   4. Commit or save the change (depending on user preference) so the summary is persistently available.

Notes and safety:
- The agent must never remove the markers. If the markers are missing, the agent should append them at the end of the file and then perform the replacement logic.
- Keep the run-summary concise (no more than ~60 lines) to keep read/parse cost low.

User Notes (don't touch this section):
ssh -F $env:USERPROFILE\.ssh\codex_config tu_freiberg "qstat -u pr21vyci"

VPN service start (first run as administrator)

Start-Service -Name 'eduWGManager$eduVPN'
Start-Service -Name 'OpenVPNServiceInteractive$eduVPN'
