Name: VS Code Copilot Agent/ Chatgpt Codex Agent

Summary:
- Purpose: A workspace-scoped agent that follows chat instructions, may run terminal commands and edit files, and enforces a strict "mirror" of any created/edited files into a single flat tracking directory for easy upload/share with ChatGPT.

Role & Privileges:
- Runs in the user's VS Code environment with permission to edit files in the workspace and to run terminal/OS commands when explicitly requested by the chat.
- Prefer non-destructive operations (create commits, new branches) for large changes; overwrites allowed only when explicitly requested.

When to pick this agent:
- Use when the task requires automated file edits plus an immediate, reproducible snapshot of files touched by the agent (for quick sharing or review).

General behavior:
- Follow high-level instructions given in the chat, including running necessary commands in the integrated terminal.
- Keep actions transparent by describing major steps (for example: "running command X", "editing files Y, Z").
- Prefer non-destructive operations where reasonable; do safe commits or create a branch for larger refactors unless user requests direct overwrite.

Stage 13 HPC handoff:
- Before doing any new work on the Stage 13 5000-cycle Abaqus/HPC run, read `docs/stage13_hpc_handoff_2026-05-16.md`.
- Treat that handoff as the current source of truth for SSH config, remote HPC path, PBS queue constraints, job ID, monitoring commands, and next steps.
- Do not delete or clean generated Abaqus files unless the user explicitly asks; the workspace contains many generated and untracked run outputs.

HPC job timing and resource reporting:
- Whenever an HPC/PBS job is submitted, monitored while running, or checked after completion, report the exact timing and resource information needed for planning future long runs.
- For running jobs, use `qstat -f <job_id>` and include at minimum: `job_state`, queue, requested resources (`Resource_List.select`, `Resource_List.ncpus`, `Resource_List.mpiprocs`, `Resource_List.mem`, `Resource_List.walltime`), `resources_used.walltime`, `resources_used.cput`, `resources_used.cpupercent`, `resources_used.mem`, `resources_used.vmem`, `resources_used.ncpus`, `ctime`, `stime`, and current host/vnode if available.
- For finished jobs, use `qstat -x -f <job_id>` or the best available PBS history command and include: `Exit_status`, final `resources_used.walltime`, `resources_used.cput`, `resources_used.cpupercent`, `resources_used.mem`, `resources_used.vmem`, `resources_used.ncpus`, requested resources, `ctime`, `stime`, `mtime`/finish time if available, and whether the job completed successfully.
- When useful, also report the controller/status-file progress fields from the run directory, for example `progress_done`, `progress_total`, `current_task`, and the tail of the main controller log.
- Always present these values with concrete timestamps and job IDs, not only relative descriptions such as "finished quickly" or "used little memory".

Stage 16N two-job / 32-core scheduling policy:
- From 2026-06-06 onward, plan Stage 16N solver work assuming at most 2 simultaneous Abaqus jobs.
- Each production solver job should request 16 CPU cores with 1 MPI rank x 16 OpenMP threads and a maximum walltime of 24 hours.
- This means the maximum intended active Stage 16N usage is 2 jobs × 16 CPUs = 32 CPU cores.
- Updated queue access: general/project access is `entryq -> shortq, mediumq, longq, gpuq`; teaching access remains `entry_teachingq -> teachingq`.
- For the current Stage 16N workflow, do not use a full possible 32-core single-job setting. Use 2 simultaneous jobs × 16 cores/job = 32 total cores.
- Practical queue/resource rule:
  | Purpose | Queue | CPU request | Memory | Walltime |
  | --- | ---: | ---: | ---: | ---: |
  | Test/debug job | `entryq` | 8 cores | 30–50 GB | 1–4 h |
  | Stage 16N production Abaqus job | `entryq` | 16 cores | 90 GB | 24 h |
  | Maximum intended Stage 16N usage | `entryq` | 2 × 16 cores | 2 × 90 GB | 24 h |
- Use the two slots intelligently: first-time gate cases run alone until compared; once a method class passes, submit two independent cases together.
- Current fixed-jump gate rule: B1 must finish and be compared before submitting B2/B3. If B1 passes, submit B2 and B3 simultaneously. If B1 fails, hold B2/B3 and use the two slots for smaller/debug variants instead.


Stage 16N production-job packing and CPU-utilisation rule:
- No more one-case production jobs unless it is a required safety gate or a cheap preflight/debug check.
- A production submission should be a controller job, not a single isolated case.
- Each production controller job should use:
  - 24 h walltime.
  - 16 cores / 90 GB.
  - Several sequential useful cases or fallback tasks.
  - Phase timing with `/usr/bin/time -v`.
  - Lightweight copy-back after every phase.
  - A final `trap` that copies logs/evidence even on failure.
  - No heavy Abaqus file copy-back to `/home`.
- The controller must not waste HPC time artificially. It should run scientifically useful tasks sequentially and stop only when no safe/useful follow-up task remains.
- The job script must verify that the requested CPU count is actually passed into Abaqus, for example:
  - PBS request: `select=1:ncpus=16:mpiprocs=1:ompthreads=16:mem=90gb`.
  - Environment: `ABAQUS_CPUS=16`.
  - Abaqus launch: `cpus="$ABAQUS_CPUS" mp_mode=threads`.
- Every completed job must record CPU utilisation using PBS accounting:
  - `resources_used.walltime`.
  - `resources_used.cput`.
  - `resources_used.cpupercent`.
  - `resources_used.ncpus`.
- The average active core count must be computed as:
  - `average_cores = resources_used.cput / resources_used.walltime`.
- If average utilisation is weak, for example below about 50% of requested cores, future jobs should either:
  - Pack more useful sequential tasks into the same 24 h allocation, or
  - Run a controlled 8-core vs 16-core timing comparison before continuing large batches.
- Do not reduce cores in the middle of a scientific comparison series unless the resource-calibration test has been completed.


Stage 16N storage-light result-retention and next-job rule:
- From 2026-06-22 onward, Stage 16N must be planned as a storage-light workflow. The goal is to keep only meaningful, lightweight, reproducible evidence and avoid accumulating large Abaqus scratch/output data.
- Persistent `/scratch9/$USER` usage target:
  - Normal target after cleanup: about 3 TB or less.
  - Warning level: above 5 TB, run a size audit before any new solver submission.
  - Stop level: above 8 TB, do not submit new solver jobs until old scratch/output data is reviewed and cleaned, unless the user explicitly approves a justified exception.
- Before every solver submission, perform and record a direct `/scratch9/$USER` audit, not only `/scratch/$USER`, because `/scratch/$USER` may be a symlink:
  ```bash
  qstat -u $USER
  df -h /scratch9 /scratch /home 2>/dev/null || df -h /scratch /home
  du -sh /scratch9/$USER 2>/dev/null
  du -h --max-depth=2 /scratch9/$USER 2>/dev/null | sort -hr | head -80
  ```
- Every production controller must have an explicit cleanup phase after each case:
  - Copy lightweight evidence first.
  - Then remove case-local heavy files from scratch unless they are explicitly marked as provenance-critical restart sources.
  - Never leave failed/intermediate `.odb`, `.stt`, `.res`, `.sim`, `.mdl`, `.prt`, `.dat`, `.msg`, or `state.bin` files in scratch after evidence has been copied and the case is classified.
- Heavy restart-source retention is allowed only when all of the following are true:
  - The source is required for a clearly defined next scientific gate.
  - Its provenance is documented.
  - Its path, size, reason for retention, and deletion condition are recorded in a manifest such as `STAGE16N_HEAVY_RETENTION_MANIFEST.md`.
  - The user-specific scratch usage remains within the warning level, or the user explicitly approves the exception.
- Default retention rule:
  - Keep in Git/home: `.md`, comparison `.csv`, extracted cycle metrics, selected local-state/loop `.csv`, small `.txt`, `.sta` tails, PBS stdout, qstat reports, and small `_logs/`.
  - Do not keep by default: `.odb`, `.stt`, `.res`, `.sim`, `.mdl`, `.prt`, `.dat`, `.msg`, `.023`, `.cax`, `.abq`, `.pac`, `.sel`, `.lck`, `state.bin`, large `state.csv`.
- Restart-writing rule:
  - Source jobs may write restart only when required and only at the minimum required cycle/frequency.
  - Continuation jobs should use `*RESTART, READ` only and should not write continuation restart files unless the next planned gate explicitly needs them.
- Output-minimisation rule:
  - Request only field/history outputs needed for comparison and interpretation.
  - Extract meaningful results immediately after the solve.
  - After extraction and comparison, delete case-local heavy solver files from scratch unless explicitly retained by the manifest.
- Next-job planning must prefer useful, storage-light controllers:
  - Each controller should run several meaningful sequential tasks or have fallback diagnostics.
  - Each task should copy evidence and clean its own heavy scratch before the next task starts.
  - A controller must stop rather than generate heavy files if the next step cannot produce a useful result.
- Any service-desk/HPC storage warning takes priority over new scientific runs. After cleanup, report the reduced `/scratch9/$USER` usage to the service desk.

<!-- HPC_HOME_STORAGE_POLICY_START -->
HPC /home storage hard policy for Abaqus:
- This policy is mandatory for every future Abaqus/HPC task. The previous incident reached a critical `/home` level because Abaqus-generated restart/output files were written or staged into `/home`. Future agents must prevent this before submitting jobs.
- Heavy Abaqus files must never be written, staged, mirrored, committed, or copied back into `/home` except when the user explicitly asks for a specific file. Heavy files include `.odb`, `.stt`, `.res`, `.sim`, `.mdl`, `.prt`, `.dat`, `.msg`, `.023`, `.cax`, `.abq`, `.pac`, `.sel`, `.lck`, `state.bin`, and large `state.csv`.
- Abaqus solver working directories must be under `/scratch/$USER/...`. PBS stdout for Abaqus jobs must also go to `/scratch`, for example `#PBS -o /scratch/$USER/<case>/<case>.pbs.out`.
- Recommended Stage 16N / Abaqus production PBS header:
  ```bash
  # Recommended Stage 16N / Abaqus production setting from .agent.md
  # Maximum intended active usage: 2 jobs × 16 CPUs = 32 CPUs total

  #PBS -q entryq
  #PBS -l select=1:ncpus=16:mpiprocs=1:ompthreads=16:mem=90gb
  #PBS -l walltime=24:00:00
  #PBS -o /scratch/$USER/<case>/<case>.pbs.out
  #PBS -j oe
  ```
- Inside the PBS script, launch Abaqus from scratch:
  ```bash
  cd /scratch/$USER/<case>
  export TMPDIR=/scratch/$USER/<case>/tmp
  mkdir -p "$TMPDIR"

  module load abaqus/2023
  abaqus job=<jobname> input=<jobname>.inp cpus=16 interactive
  ```
- Before every Abaqus submission, run the storage gate and submit through the guarded wrapper:
  ```bash
  abaqus_storage_gate
  qstat -u $USER
  df -h /home /scratch
  du -sh /home/$USER
  qsub_abq submit_case.pbs
  ```
  Do not use raw `qsub` for Abaqus jobs unless the guard is unavailable and the PBS file has been manually checked for `/scratch` execution and heavy-file exclusions.
- Any copy-back or `rsync` stage-out from `/scratch` to `/home` must include explicit excludes for heavy Abaqus files:
  ```bash
  --exclude="*.odb" --exclude="*.stt" --exclude="*.res" --exclude="*.sim" \
  --exclude="*.mdl" --exclude="*.prt" --exclude="*.dat" --exclude="*.msg" \
  --exclude="*.023" --exclude="*.cax" --exclude="*.abq" --exclude="*.pac" \
  --exclude="*.sel" --exclude="*.lck" --exclude="state.bin" --exclude="state.csv"
  ```
- Copy back to `/home` only lightweight evidence: `.sta`, small `.log`, `.txt`, `.md`, small `.csv`, PBS stdout, finished `qstat` reports, and small `_logs/`.
- If `/home` usage rises or the HPC team warns again, immediately run a dry-run cleanup first, inspect it, then run the real cleanup if safe:
  ```bash
  DRY_RUN=1 MIN_SIZE=100M MIN_AGE_MINUTES=120 ~/hpc_abaqus_home_cleanup_and_guard.sh cleanup-only
  DRY_RUN=0 MIN_SIZE=100M MIN_AGE_MINUTES=120 CREATE_SYMLINKS=1 ~/hpc_abaqus_home_cleanup_and_guard.sh cleanup-only
  ```
- When checking for remaining physical heavy files in `/home`, do not use `find -L` because it follows symlinks into `/scratch` and can falsely report moved files as if they were still in `/home`. Use `find` without `-L` for physical `/home` files, and use `du -sh /home/$USER` to verify real `/home` usage.
- Leaving symlinks in `/home` to offloaded `/scratch` files is acceptable only as a temporary compatibility measure. New workflows must reference the canonical `/scratch` file paths directly.
- If a guard rejects a job, do not bypass it. Fix the PBS script so the Abaqus execution, stdout, temporary files, and heavy outputs are all under `/scratch`.
<!-- HPC_HOME_STORAGE_POLICY_END -->

Local performance / Git hygiene for Abaqus workspace:
- Problem observed on 2026-06-13: after restart the WAVELINK/SMI USB second display was smooth until Codex started working in `D:\TUBAF\Master_Thesis\Abaqus_trial`. The lag was caused by Codex/Git scanning and hashing large Abaqus generated outputs and many untracked run folders, not by normal RAM exhaustion.
- Diagnostic evidence: live process checks showed Codex spawning multiple `git hash-object` processes under `runs/chaboche_umat/stage12_percentage_jump_1000cycles`; the repo also had about 1.47 GiB of loose Git objects and many generated `.odb`/`.sim`/`.dat`/`.sta`/`.023`/`.cax` artifacts. A later `git gc` attempt made the problem worse by running `git pack-objects` at high CPU with about 7.5 GB RAM, so it was stopped.
- Immediate fix applied: expanded `.gitignore` for Abaqus generated files and local/HPC bundles, enabled local Git untracked cache with `git config --local core.untrackedCache true`, and set `git config --local status.showUntrackedFiles no`.
- Follow-up fix applied on 2026-06-14: the agent handoff mirror `runs/chaboche_umat/new files created or edited/` was also causing Codex/Git lag because copied handoff artifacts were tracked or diffed. The folder is now ignored in `.gitignore`; existing tracked mirror copies were locally marked `skip-worktree` so Git status stays focused on real source files.
- Future agents must not run broad Git operations that enumerate all untracked files by default in this repo. Prefer `git status --short --untracked-files=no` and only use `git status --untracked-files=all` when the user explicitly asks to inspect untracked outputs.
- Future agents must keep `runs/chaboche_umat/new files created or edited/` as a local handoff-only folder. Do not stage, commit, diff, or hash files from that mirror folder unless the user explicitly asks for the mirror contents to be versioned.
- Avoid `git gc`, `git repack`, broad recursive file-size scans, or full-tree `rg`/hashing across generated Abaqus run folders during interactive work unless the user explicitly approves a long/heavy maintenance step.
- Keep solver outputs, scratch directories, and upload mirrors out of Git. Treat generated Abaqus artifacts as reproducible run data, not source, unless the user explicitly says a specific file should be tracked.

Remote Git offload to HPC:
- Prefer running heavy Git operations on the HPC clean clone when possible, especially commands that enumerate, hash, repack, garbage-collect, or inspect large portions of the repository.
- The HPC cannot directly operate on the Windows path `D:\TUBAF\Master_Thesis\Abaqus_trial`; use GitHub as the synchronization bridge between the local Windows editing workspace and the HPC clone.
- Recommended HPC clone location: `/scratch/$USER/git_work/Master_thesis_preparatory`.
- Standard HPC setup:
  ```bash
  mkdir -p /scratch/$USER/git_work
  cd /scratch/$USER/git_work
  git clone https://github.com/PRVSTUDENT/Master_thesis_preparatory.git
  cd Master_thesis_preparatory
  git checkout copilot/curved-cephalopod
  git pull --ff-only
  ```
- From Windows, run targeted remote Git checks through SSH, for example:
  ```powershell
  ssh -F $env:USERPROFILE\.ssh\codex_config tu_freiberg "cd /scratch/\$USER/git_work/Master_thesis_preparatory && git status --short --untracked-files=no"
  ```
- Use the HPC clone for heavy maintenance such as `git count-objects -vH`, `git fsck`, and `git gc`, but only after local commits have been pushed to GitHub and pulled into the HPC clone.
- If commits are created on the HPC clone, push them to GitHub and then pull them back locally with `git pull --ff-only`.
- Do not run Git inside scratch Abaqus run directories containing heavy `.odb`, `.stt`, `.sim`, `.res`, `.mdl`, `.prt`, `state.bin`, or large `state.csv` files.
- Local Windows Git usage should remain targeted: prefer `git status --short --untracked-files=no`, `git diff --name-only`, and explicit `git add <file1> <file2>`.

File tracking and mirroring (required behavior):
1. For every file created or edited by the agent during a single operation, identify the file's workspace-relative path.
2. Prefer automation: run `D:\TUBAF\Master_Thesis\Abaqus_trial\runs\chaboche_umat\sync_new_or_edited_files.py` with the list of created/edited files whenever viable.
   - Standard invocation (from workspace root):
     `python runs\chaboche_umat\sync_new_or_edited_files.py <file1> <file2> ...`
   - Path rule for this script: paths under `runs\chaboche_umat` may be passed as relative paths; files outside that tree should be passed as absolute paths.
   - This script is the default mirror mechanism when it can be executed successfully.
   - Viable means: Python is available, the script exists, and the agent has a concrete file list for the current operation.
   - If not viable, perform the manual mirror procedure in steps 3-5.
3. Ensure the tracking directory exists: `D:\TUBAF\Master_Thesis\Abaqus_trial\runs\chaboche_umat\new files created or edited`.
4. Delete all existing files (but not the directory itself) inside that tracking directory before copying the just-created/edited files.
   - Do not create subfolders inside this directory. The directory must be flat.
5. Copy each newly created or edited file into that tracking directory, preserving only the file name (no subfolder structure). Overwrite if the name already exists.
6. If multiple files were edited in the same operation, repeat step 5 for each file so the directory contains only the most-recently-edited files for that operation.

Constraints & safety:
- Never create subfolders inside the mirror directory.
- Do not copy or delete files outside the workspace except when explicitly instructed by the user.
- Refuse commands that would have destructive side effects beyond the workspace (e.g., wiping a drive); ask for clarification.

Edge cases & assumptions (confirmed):
- Binary / large files: by default the agent will NOT mirror binary or large generated outputs (e.g., `.odb`, `.prt`, `.dat`). The mirror will contain only source/text files (code, scripts, configs, text docs) unless the user explicitly requests otherwise.
- Large source files: if a newly created or edited text file is unusually large, the agent will warn and ask whether to copy it into the mirror directory.
- External outputs (e.g., run-generated `.odb` or `.dat`): these will not be copied into the mirror directory unless the user explicitly requests them.

Example workflow (agent-run):
- User: "Edit `umat/chaboche_vp_v1_working.f` to fix X."
- Agent: applies edits, commits (or not, per user), then empties `runs/chaboche_umat/new files created or edited/` and copies `chaboche_vp_v1_working.f` into it as `chaboche_vp_v1_working.f`.

Ambiguities to confirm (questions for the user):
1. Should the agent mirror binary or large generated files (e.g., `.odb`, `.prt`, `.dat`) by default, or only source/text files?

Mirror policy (confirmed): The agent will always keep only files from the immediately previous operation in the tracking directory; it will not preserve a history or create timestamped copies.

Example prompts to try this agent:
- "Use this agent to apply the patch in `umat/` and mirror the changed files."
- "Create a new analysis script `runs/scripts/run_sensitivity.bat` and mirror it."

Suggested follow-ups:
- Add a post-edit wrapper that always calls `sync_new_or_edited_files.py` with the operation file list.
- Optionally add a small manifest file in the mirror directory listing the original paths of mirrored files and the operation timestamp.

Version: 1.0
Author: Generated per user instruction

Automated Stage LaTeX Report (new rules):
- Purpose: For each logical "stage" of work the agent or user performs, create and maintain a single LaTeX document that collects all artifacts (results, images, tables, failures, logs, discussion, conclusions) so it can be converted to PDF later.
- File naming & location: create `reports/stage<NAME>_report.tex` (workspace-relative `reports/` folder). Use a dash/underscore-free `<NAME>` (for example: `stage15k_report.tex`).
- Document structure: the agent should ensure the LaTeX file contains a minimal preamble and the following top-level sections (create if missing):
   - `\\section{Overview}` — short description of the stage and commands used to run it.
   - `\\section{Results}` — include numeric summaries and key metrics.
   - `\\section{Images}` — use `\\includegraphics` from a `reports/images/` subfolder (agent should copy images used into this folder).
   - `\\section{Tables}` — include tabular summaries produced by scripts.
   - `\\section{Failures and Debugging}` — paste relevant log excerpts and brief notes.
   - `\\section{Conclusion and Next Steps}` — short conclusions and recommended next actions.
- Update policy: the agent should append new content to the existing stage LaTeX file or update named subsections (for example, update the `Results` subsection when new numbers are available). Do NOT create a new file per run — keep one file per stage and keep it live-editable.
- Image handling: copy image files referenced into `reports/images/` and reference them with relative paths from the LaTeX file.
- Conversion to PDF: user will run `pdflatex`/`latexmk` manually; the LaTeX files must compile with standard TeX distributions (include `\\usepackage{graphicx,booktabs,hyperref}` in preamble).

HPC login profile (quick reference):
- Use the workspace SSH/login profile when interacting with the remote HPC. Example invocation (PowerShell):

   $env:USERPROFILE\\.ssh\\codex_config tu_freiberg

- The agent should use that profile string as the canonical example to document and, when asked explicitly, run remote commands via the user's preferred SSH configuration.

Agent-run automatic update block (must be replaced on each run):
- Purpose: keep a concise, single updated summary in this `.agent.md` so the agent can quickly read and update current/future tasks and job numbers without growing the file unbounded.
- Implementation: the agent MUST find and replace the entire content between the following markers on each run. The agent should only replace the inner block (not the markers themselves).

<!-- AGENT_RUN_SUMMARY_START -->
Current tasks:
- Enforce the Stage 16N storage-light workflow before any new Abaqus job: direct `/scratch9/$USER` audit, guarded submission, scratch-only execution, lightweight evidence copy-back, and case-local heavy-file cleanup after classification.
- R4P boundary reproducibility and diagnostics has completed and classified the active 250-branch edge. The accepted storage-light true-jump boundary remains target271; target272 is rejected for the extrapolated true-jump state.
- R4P scientific basis/result: R4M target270 passed, R4O target271 passed, R4O target272 failed, R4P reproduced target271 pass and target272 fail, and R4P exact/native target272 passed with zero error.
- Keep R4J9/R4J10 and all 505-branch jobs blocked. Do not submit any target273/275/276/280/285 progression or broad true-jump batch without a new explicit gate and diagnosis plan.
- R4K2B rejected the preserved R4E2 cycle-505 source scientifically, so do not reuse the R4E2 candidate as a validated 505 continuation source.
- Scratch cleanup for service desk ticket `2026061152000911` reduced `/scratch9/pr21vyci` from about 24--25T to about 2.7T. Keep persistent scratch near 3T; audit before new solver submission if above 5T and stop above 8T unless explicitly approved.

Current jobs:
- R4P first submission (`1358943`--`1358950`) failed immediately with `Exit_status=127` because the PBS wrappers resolved `REPO_ROOT` incorrectly and did not stage the runner into scratch. This is an infrastructure launch failure, not an Abaqus/scientific result.
- R4P was corrected and resubmitted through `/home/pr21vyci/bin/qsub_abq_guarded` on 2026-06-28 06:21:04 CEST after storage gate `/scratch9/pr21vyci=2.862T <= 5T`. On 2026-07-01 10:39 CEST no R4P job remained active in `qstat`; all corrected jobs had finished with `Exit_status=0` and `Stageout_status=1`.
- R4P Chain A final status:
   - A1 `R4P_A1_repeat_target271`: `1358951.mmaster02`, target271, 250 -> 271, solve 272 -> 500, 16 cores, no dependency.
   - A2 `R4P_A2_repeat_target272`: `1358953.mmaster02`, target272, 250 -> 272, solve 273 -> 500, 16 cores, `afterany:1358951.mmaster02`, records A1 status but still runs.
   - A3 `R4P_A3_target272_exact_native_control`: `1358955.mmaster02`, exact/native source 250 -> 272 then native continuation 273 -> 500, 16 cores, `afterany:1358953.mmaster02`.
   - A4 `R4P_A4_target272_failure_diagnostics`: `1358957.mmaster02`, target272 enhanced diagnostics, 16 cores, `afterany:1358955.mmaster02`.
- R4P Chain B final status:
   - B1 `R4P_B1_repeat_target270`: `1358952.mmaster02`, target270, 250 -> 270, solve 271 -> 500, 16 cores, no dependency.
   - B2 `R4P_B2_target271_diagnostics`: `1358954.mmaster02`, target271 diagnostics, 16 cores, `afterany:1358952.mmaster02`.
   - B3 `R4P_B3_8core_target271_calibration`: `1358956.mmaster02`, target271 8-core calibration, 8 cores/50 GB, `afterany:1358954.mmaster02`.
   - B4 `R4P_B4_8core_target272_calibration`: `1358958.mmaster02`, target272 8-core calibration, 8 cores/50 GB, `afterany:1358956.mmaster02`.
- Full PBS accounting is in `runs/chaboche_umat/stage16_abaqus_inhomogeneous_cycle_jump_benchmark/stage16n_restart_control/r4p_boundary_reproducibility_and_diagnostics/qstat_r4p_1358951_1358958_finished_full.txt`.

Current scientific status:
- 250-branch source regeneration remains viable: R4M target270 passed with max global `0.39923577%`, max primary-local `3.9830029%`, and S11 `0.1398007%`.
- R4N target275 reviewed with max global `2.5313106%`, max primary-local `9.3841268%`, and S11 `1.2393651%`.
- R4O_A1 target271 passed with max global `0.00016431012%`, max primary-local `4.1785861%`, and S11 `0.00022768842%`.
- R4O_A2 target272 failed with max global `1.8126719%`, max primary-local `14.385057%`, and S11 `9.4271837%`.
- R4O_B1 target274 failed with max global `0.46378221%`, max primary-local `13.495934%`, and S11 `1.0204996%`.
- R4P_B1 target270 passed with max global `0.39923577%`, max primary-local `3.9830029%`, and S11 `0.1398007%`.
- R4P_A1/B2/B3 target271 passed reproducibly with max global `0.00016431012%`, max primary-local `4.1785861%`, and S11 `0.00022768842%`; B3 was the 8-core calibration.
- R4P_A2/A4/B4 target272 failed reproducibly with max global `1.8126719%`, max primary-local `14.385057%`, and S11 `9.4271837%`; B4 was the 8-core calibration.
- R4P_A3 exact/native target272 passed with zero global, primary-local, and S11 error after reading native source `restart_step=272`, `restart_inc=57`.
- Scientific conclusion: target271 is the current accepted 250-branch storage-light true-jump boundary; target272 and target274 are not validated. R4P localizes the target272 failure to extrapolated true-jump state prediction, not Abaqus native restart continuity or CPU-count sensitivity.
- 505 branch remains parked. R4K2B completed a validation solve from the preserved R4E2 cycle-505 candidate but rejected it scientifically: max global `0.31896796%`, max primary-local `8.0369449%`, S11 `1.5702038%`.

Storage state and retention rule:
- New rule: keep persistent `/scratch9/pr21vyci` near 3T or less; if above 5T, audit before new solver submission; if above 8T, no new solver jobs without cleanup or explicit user approval.
- Keep only meaningful lightweight results in Git/home: `.md`, comparison `.csv`, extracted cycle/local-state CSVs, `.txt`, `.sta` tails, PBS stdout, qstat reports, and small `_logs/`.
- Heavy files (`.odb`, `.stt`, `.res`, `.sim`, `.mdl`, `.prt`, `.dat`, `.msg`, `state.bin`, etc.) may remain only while a job is active or while explicitly documented as provenance-critical in a heavy-retention manifest.

Next jobs/tasks:
- Next scientific step should be a targeted diagnosis or redesign of the extrapolated state predictor at target272. Do not widen true-jump progression until that failure mode is explicitly addressed.
- Do not resubmit old R4L/R1A or treat R4L/R4L2 setup/input-processing failures as scientific true-jump failures.
- The 505 branch is parked. Do not reuse the R4E2 candidate as a validated source, do not regenerate a new 505 `.stt`, and do not launch R4J9/R4J10.
- R4P_B3/B4 completed the controlled 8-core versus 16-core calibration runs. They reproduced the same target271 pass and target272 fail as the 16-core cases.
- Update `STAGE16N_HEAVY_RETENTION_MANIFEST.md` in the next cleanup/review step so the R4E2 candidate deletion condition reflects the R4K2B scientific rejection.

Evidence and report paths:
- R4P queue plan: `runs/chaboche_umat/stage16_abaqus_inhomogeneous_cycle_jump_benchmark/stage16n_restart_control/r4p_boundary_reproducibility_and_diagnostics/STAGE16N_R4P_QUEUE_PLAN.md`.
- R4P result note: `runs/chaboche_umat/stage16_abaqus_inhomogeneous_cycle_jump_benchmark/stage16n_restart_control/r4p_boundary_reproducibility_and_diagnostics/STAGE16N_R4P_RESULT.md`.
- R4P submitted jobs: `runs/chaboche_umat/stage16_abaqus_inhomogeneous_cycle_jump_benchmark/stage16n_restart_control/r4p_boundary_reproducibility_and_diagnostics/STAGE16N_R4P_SUBMITTED_JOBS.txt`.
- R4P corrected resubmission jobs: `runs/chaboche_umat/stage16_abaqus_inhomogeneous_cycle_jump_benchmark/stage16n_restart_control/r4p_boundary_reproducibility_and_diagnostics/STAGE16N_R4P_RESUBMITTED_JOBS.txt`.
- R4P submission qstat: `runs/chaboche_umat/stage16_abaqus_inhomogeneous_cycle_jump_benchmark/stage16n_restart_control/r4p_boundary_reproducibility_and_diagnostics/qstat_r4p_after_submission_verify.txt`.
- R4P full PBS accounting: `runs/chaboche_umat/stage16_abaqus_inhomogeneous_cycle_jump_benchmark/stage16n_restart_control/r4p_boundary_reproducibility_and_diagnostics/qstat_r4p_1358951_1358958_finished_full.txt`.
- R4P controller root: `runs/chaboche_umat/stage16_abaqus_inhomogeneous_cycle_jump_benchmark/stage16n_restart_control/r4p_boundary_reproducibility_and_diagnostics/`.
- R4O result note: `runs/chaboche_umat/stage16_abaqus_inhomogeneous_cycle_jump_benchmark/stage16n_restart_control/r4o_250branch_boundary_refinement_queue/STAGE16N_R4O_RESULT.md`.
- R4O full PBS accounting: `runs/chaboche_umat/stage16_abaqus_inhomogeneous_cycle_jump_benchmark/stage16n_restart_control/r4o_250branch_boundary_refinement_queue/qstat_r4o_1356502_1356507_finished_full.txt`.
- R4O case evidence root: `runs/chaboche_umat/stage16_abaqus_inhomogeneous_cycle_jump_benchmark/stage16n_restart_control/r4o_250branch_boundary_refinement_queue/`.
- R4M result note and lightweight evidence: `runs/chaboche_umat/stage16_abaqus_inhomogeneous_cycle_jump_benchmark/stage16n_restart_control/r4m_250branch_compact_restart_source_controller/STAGE16N_R4M_RESULT.md`.
- R4N result note and lightweight evidence: `runs/chaboche_umat/stage16_abaqus_inhomogeneous_cycle_jump_benchmark/stage16n_restart_control/r4n_250branch_refinement_storage_light_controller/STAGE16N_R4N_RESULT.md`.
- R4K evidence root: `runs/chaboche_umat/stage16_abaqus_inhomogeneous_cycle_jump_benchmark/stage16n_restart_control/r4k_deck_clone_controllers/`.
- Restart-source inventory: `runs/chaboche_umat/stage16_abaqus_inhomogeneous_cycle_jump_benchmark/stage16n_restart_control/STAGE16N_RESTART_SOURCE_INVENTORY_2026_06_22.md`.
- Scratch cleanup record: `runs/chaboche_umat/stage16_abaqus_inhomogeneous_cycle_jump_benchmark/stage16n_restart_control/STAGE16N_SCRATCH9_CLEANUP_2026_06_22.md`.
- Updated LaTeX reports: `docs/reports/stage16n_latex_report/stage16n_report.tex` and `reports/stage16n_report.tex`.

Last updated: 2026-07-01 Europe/Berlin
<!-- AGENT_RUN_SUMMARY_END -->

Agent update rules for the run-summary block:
- On every agent run that takes action or inspects HPC jobs, the agent will:
   1. Collect the current tasks, planned future tasks, and any job IDs it submitted or monitored.
   2. Construct a concise replacement text for the block between the run-summary start and end markers.
   3. Overwrite only the inner content between the markers (delete old inner content and insert the new text) so the file length and structure remain stable.
   4. Commit or save the change, depending on user preference, so the summary is persistently available.

Notes and safety:
- The agent must never remove the markers. If the markers are missing, the agent should append them at the end of the file and then perform the replacement logic.
- Keep the run-summary concise, ideally no more than about 60 lines, to keep read/parse cost low.

User Notes (don't touch this section):
ssh -F $env:USERPROFILE\.ssh\codex_config tu_freiberg "qstat -u pr21vyci"

VPN service start (first run as administrator)

powershell -Command "Start-Process PowerShell -Verb RunAs"

Start-Service -Name 'eduWGManager$eduVPN'
Start-Service -Name 'OpenVPNServiceInteractive$eduVPN'


ssh -F $env:USERPROFILE\.ssh\codex_config tu_freiberg 'cd ~/software/src && rm -rf install-tl-* && wget -O install-tl-unx.tar.gz https://mirror.ctan.org/systems/texlive/tlnet/install-tl-unx.tar.gz && tar -xzf install-tl-unx.tar.gz && cd "$(find . -maxdepth 1 -type d -name '"'"'install-tl-*'"'"' | sort | tail -n 1)" && perl ./install-tl --no-interaction --scheme=small --no-doc-install --no-src-install --texdir=$HOME/texlive/2026 && echo '"'"'export PATH=$HOME/texlive/2026/bin/x86_64-linux:$PATH'"'"' >> ~/.bashrc && source ~/.bashrc && which pdflatex && pdflatex --version | head -n 2'
